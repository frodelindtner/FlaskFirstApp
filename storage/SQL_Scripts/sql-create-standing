FAILME 

CREATE TABLE Teams(
	Id INTEGER PRIMARY KEY AUTOINCREMENT,
	Season INTEGER,
	SeasonType INTEGER,
	City VARCHAR(50),
	Name VARCHAR(50),
	ShortNameLeague VARCHAR(20),
	Division VARCHAR(50)
);

CREATE TABLE Results(
	Id INTEGER PRIMARY KEY AUTOINCREMENT,
	TeamId INTEGER NOT NULL,
	Wins INTEGER, 
	Losses INTEGER,
	NightWins INTEGER,		
	FOREIGN KEY (TeamId) REFERENCES Teams(Id)
);

